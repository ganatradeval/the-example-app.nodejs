version: 2
jobs:
  build-and-test:
    docker:
      - image: circleci/node:8.0
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
            - run: npm test
workflows:
    build-and-test:
      jobs:
        - build-and-test
