version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.0
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
            - run: npm test
